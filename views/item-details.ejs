<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
  <title>Item Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="/src/table.css" rel="stylesheet"/>
  <link rel="stylesheet" href="/fa/all.min.css">
</head>
<body class="bg-gray-100 font-sans">
  <div class="min-h-screen flex items-center justify-center p-4">
    <div class="bg-white rounded-lg shadow-lg p-6 w-full max-w-2xl">
      <h1 class="text-2xl font-bold text-center mb-6 text-blue-700">Item Details</h1>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h2 class="text-xl font-semibold mb-4">Basic Information</h2>
          <div class="space-y-3">
            <div>
              <span class="font-medium">Property No:</span>
              <span><%= item.property_no || 'N/A' %></span>
            </div>
            <div>
              <span class="font-medium">Description:</span>
              <span><%= item.description || 'N/A' %></span>
            </div>
            <div>
              <span class="font-medium">Department:</span>
              <span><%= item.department_name || 'N/A' %></span>
            </div>
            <div>
              <span class="font-medium">Date Acquired:</span>
              <span><%= item.date_acq ? new Date(item.date_acq).toLocaleDateString() : 'N/A' %></span>
            </div>
          </div>
        </div>
        
        <div>
          <h2 class="text-xl font-semibold mb-4">Technical Details</h2>
          <div class="space-y-3">
            <div>
              <span class="font-medium">Quantity:</span>
              <span><%= item.qty || 'N/A' %></span>
            </div>
            <div>
              <span class="font-medium">Unit Value:</span>
              <span><%= item.unit_of_value || 'N/A' %></span>
            </div>
            <div>
              <span class="font-medium">Condition:</span>
              <span class="capitalize"><%= item.condition_code || 'N/A' %></span>
            </div>
            <div>
              <span class="font-medium">Scan Status:</span>
              <% if (item.scan_status === 'first_scan') { %>
                <span class="bg-blue-100 text-blue-800 px-2 py-1 rounded text-xs">
                  First Scan: <%= item.first_scan_date ? new Date(item.first_scan_date).toLocaleDateString() : 'N/A' %>
                </span>
              <% } else if (item.scan_status === 'second_scan') { %>
                <span class="bg-green-100 text-green-800 px-2 py-1 rounded text-xs">
                  Completed: <%= item.second_scan_date ? new Date(item.second_scan_date).toLocaleDateString() : 'N/A' %>
                </span>
              <% } else { %>
                <span class="bg-gray-100 text-gray-800 px-2 py-1 rounded text-xs">Not Scanned</span>
              <% } %>
            </div>
          </div>
        </div>
      </div>
      
      <% if (item.qr_code_path) { %>
        <div class="mt-6 text-center">
          <h2 class="text-xl font-semibold mb-4">QR Code</h2>
          <img src="<%= item.qr_code_path %>" alt="QR Code" class="w-48 h-48 mx-auto"/>
          <p class="mt-2 text-sm text-gray-600">Scan this code to view item details</p>
        </div>
      <% } %>
      
      <div class="mt-6 text-center">
        <button onclick="window.print()" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
          Print This Page
        </button>
      </div>
    </div>
  </div>
</body>
</html>