<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Print Purchase Request</title>
    <link rel="icon" href="/img/Calapan_City_Logo.png" type="image/png">
    <style>
        body {
            font-family: 'Times New Roman', serif;
            margin: 0;
            padding: 10px;
            position: relative; /* For QR code overlay positioning */
        }
        .form-container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            padding: 15px;
            background-color: #fff;
            box-sizing: border-box;
        }
        .header-container {
            position: relative; /* For QR code overlay */
            display: flex;
            justify-content: center; /* Center the header */
            align-items: center;
            margin-bottom: 15px;
        }
        .form-header {
            text-align: center;
            font-size: 18px;
            font-weight: bold;
            padding: 8px;
            border-radius: 4px;
            width: 100%; /* Ensure the header takes full width */
        }
        .qr-code-container {
            position: absolute;
            top: 0;
            right: 0;
            z-index: 10; /* Ensure QR code is above other elements */
        }
        .qr-code {
            width: 80px;
            height: 80px;
        }
        .info-table {
            width: 100%;
            margin-bottom: 10px;
            border-collapse: collapse;
        }
        .info-table td, .info-table th {
            border: 1px solid #333;
            padding: 5px;
            font-size: 12px;
            vertical-align: top;
        }
        .info-table .no-right-border {
            border-right: none;
        }
        .info-table .no-left-border {
            border-left: none;
        }
        .form-label {
            font-weight: bold;
            font-size: 12px;
        }
        .form-table {
            width: 100%;
            height: 600px;
            margin-top: 10px;
            margin-bottom: 20px;
            border-collapse: collapse;
            table-layout: fixed;
        }
        .form-table th {
            border: 1px solid #333;
            padding: 2px;
            text-align: center;
            font-size: 12px;
            font-weight: bold;
        }
        .form-table td {
            border: 1px solid #333;
            padding: 2px;
            text-align: center;
            font-size: 10px;
            word-wrap: break-word;
            vertical-align: top;
        }
        .form-table tbody tr:last-child td {
            border-bottom: 1px solid #333;
        }
        .form-table tbody tr {
            height: 20px;
        }
        .form-table .filler-row {
            height: 20px;
            visibility: hidden;
        }
        .total-section {
            text-align: right;
            font-weight: bold;
            margin-top: 10px;
            margin-bottom: 10px;
            font-size: 12px;
            padding-top: 5px;
        }
        .purpose-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
            font-size: 12px;
        }
        .footer-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 5px;
            margin-bottom: 20px; /* Add padding at the bottom */
            font-size: 12px;
        }
        .purpose-table td, .footer-table td {
            border: 1px solid #333;
            padding: 5px;
            vertical-align: top;
        }
        .footer-table .small-text {
            font-size: 12px;
        }
        .signature-cell {
            text-align: center;
            height: 80px;
            position: relative;
        }
        .signature-label {
            font-size: 12px;
            font-weight: bold;
            margin-bottom: 5px;
        }
        .signature-line {
            border-top: 1px solid #333;
            width: 80%;
            margin: 0 auto;
            position: absolute;
            bottom: 35px;
            left: 0;
            right: 0;
        }
        .signature-name {
            font-weight: bold;
            font-size: 12px;
            position: absolute;
            bottom: 15px;
            left: 0;
            right: 0;
        }
        .signature-title {
            font-size: 12px;
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
        }
        .placeholder-cell {
            text-align: center;
            height: 80px;
            position: relative;
        }
        .placeholder-label {
            font-size: 12px;
            font-weight: normal; /* Remove bold */
            position: absolute;
            bottom: 45px; /* Move closer to the bottom */
            left: 0;
            right: 0;
        }
        .placeholder-name {
            font-size: 12px;
            font-weight: normal; /* Remove bold */
            position: absolute;
            bottom: 25px; /* Move closer to the bottom */
            left: 0;
            right: 0;
        }
        .placeholder-title {
            font-size: 12px;
            font-weight: normal; /* Remove bold */
            position: absolute;
            bottom: 10px; /* Move closer to the bottom */
            left: 0;
            right: 0;
        }
        .page-break {
            page-break-after: always;
        }
        .page-number {
            text-align: center;
            font-size: 12px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <% 
    // Calculate number of pages needed
    const itemsPerPage = 38;
    const totalItems = request.items.length;
    const totalPages = Math.ceil(totalItems / itemsPerPage);
    
    for (let page = 0; page < totalPages; page++) { 
        const startIndex = page * itemsPerPage;
        const endIndex = Math.min(startIndex + itemsPerPage, totalItems);
        const currentPageItems = request.items.slice(startIndex, endIndex);
        const isLastPage = page === totalPages - 1;
    %>
    <div class="form-container <%= page > 0 ? 'page-break' : '' %>">
        <div class="header-container">
            <div class="form-header">PURCHASE REQUEST</div>
            <% if (request.status === 'e-sign') { %>
                <div class="qr-code-container">
                    <img src="<%= request.qrCodeUrl %>" class="qr-code" alt="Document Verification QR Code">
                </div>
            <% } %>
        </div>

        <table class="info-table">
            <tr>
                <td width="66.66%" colspan="2" class="no-right-border">
                    <span class="form-label">LGU:</span> CITY OF CALAPAN
                </td>
                <td width="33.33%" class="no-left-border">
                    <span class="form-label">Fund:</span> <%= request.fund %>
                </td>
            </tr>
            <tr>
                <td width="33.33%">
                    <span class="form-label">Department:</span> <%= request.department %><br>
                    <span class="form-label">Section:</span> <%= request.section %>
                </td>
                <td width="33.33%">
                    <span class="form-label">PR No.:</span> <%= request.pr_no %><br>
                    <span class="form-label">FPP:</span> <%= request.fpp %>
                </td>
                <td width="33.33%">
                    <span class="form-label">Date:</span> <%= new Date(request.date_requested).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %>
                </td>
            </tr>
        </table>

        <table class="form-table">
            <thead>
                <tr>
                    <th style="width: 5%;">Item No.</th>
                    <th style="width: 8%;">Unit</th>
                    <th style="width: 45%;">Item Description</th>
                    <th style="width: 12%;">Quantity</th>
                    <th style="width: 15%;">Unit Cost</th>
                    <th style="width: 15%;">Total Cost</th>
                </tr>
            </thead>
            <tbody>
                <% currentPageItems.forEach(item => { %>
                    <tr>
                        <td><%= item.item_no %></td>
                        <td><%= item.unit %></td>
                        <td style="text-align: left; padding-left: 5px;"><%= item.item_description %></td>
                        <td><%= item.quantity %></td>
                        <td>₱<%= parseFloat(item.unit_cost).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></td>
                        <td>₱<%= parseFloat(item.total_cost).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></td>
                    </tr>
                <% }) %>
                <% const remainingRows = itemsPerPage - currentPageItems.length; %>
                <% for (let i = 0; i < remainingRows; i++) { %>
                    <tr class="filler-row">
                        <td> </td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                        <td></td>
                    </tr>
                <% } %>
            </tbody>
        </table>

        <% if (isLastPage) { %>
        <div class="total-section">TOTAL: ₱<%= parseFloat(request.total).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 }) %></div>

        <table class="purpose-table">
            <tr>
                <td>
                    <span class="form-label">Purpose:</span> <%= request.purpose %>
                </td>
            </tr>
        </table>

        <table class="footer-table">
            <tr>
                <!-- Placeholder Signature Box -->
                <td width="16%" class="placeholder-cell">
                    <div class="placeholder-label">Signature:</div>
                    <div class="placeholder-name">Printed Name:</div>
                    <div class="placeholder-title">Designation</div>
                </td>
                <!-- Requested By -->
                <td width="28%" class="signature-cell">
                    <div class="signature-label">REQUESTED BY:</div>
                    <div class="signature-line"></div>
                    <div class="signature-name"><%= request.requested_by.name %></div>
                    <div class="signature-title"><%= request.requested_by.position %></div>
                </td>
                <!-- Cash Availability -->
                <td width="28%" class="signature-cell">
                    <div class="signature-label">CASH AVAILABILITY:</div>
                    <div class="signature-line"></div>
                    <div class="signature-name"><%= request.cash_availability.name %></div>
                    <div class="signature-title"><%= request.cash_availability.bac_position || request.cash_availability.position %></div>
                </td>
                <!-- Approved By -->
                <td width="28%" class="signature-cell">
                    <div class="signature-label">APPROVED BY:</div>
                    <div class="signature-line"></div>
                    <div class="signature-name"><%= request.approved_by.name %></div>
                    <div class="signature-title"><%= request.approved_by.bac_position || request.approved_by.position %></div>
                </td>
            </tr>
        </table>
        <% } else { %>
            <!-- Add filler space for non-last pages -->
            <div style="height: 120px;"></div>
        <% } %>
        
    </div>
    <% } %>

    <script>
        window.onload = function() {
            window.print();
        };
    </script>
</body>
</html>