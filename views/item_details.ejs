<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8"/>
  <meta content="width=device-width, initial-scale=1.0" name="viewport"/>
  <title>Item Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="/src/table.css" rel="stylesheet"/>
  <link rel="icon" href="/img/Calapan_City_Logo.png" type="image/png">
</head>
<body class="bg-blue-50 font-sans">
    <div class="w-full">
      <div class="bg-white p-6 lg:p-10 rounded-lg shadow-sm">
        <h2 class="text-2xl font-bold text-blue-700 mb-6">Item Details</h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <h3 class="text-xl font-semibold mb-4">Basic Information</h3>
            <div class="space-y-3">
              <p><span class="font-medium">ICS No:</span> <%= item.ics_no %></p>
              <p><span class="font-medium">Particular:</span> <%= item.particular %></p>
              <p><span class="font-medium">Quantity:</span> <%= item.quantity %></p>
              <p><span class="font-medium">Unit:</span> <%= item.unit %></p>
              <p><span class="font-medium">Unit Amount:</span> <%= item.unit_amount || '-' %></p>
            </div>
          </div>
          
          <div>
            <h3 class="text-xl font-semibold mb-4">Additional Information</h3>
            <div class="space-y-3">
              <p><span class="font-medium">Issued To:</span> <%= item.issued_to %></p>
              <p><span class="font-medium">Department:</span> <%= item.department %></p>
              <p><span class="font-medium">Date Encoded:</span> <%= item.date_encode %></p>
              <p><span class="font-medium">Air Date:</span> <%= item.air_date %></p>
              <p><span class="font-medium">Estimated Useful Life:</span> <%= item.est_useful_life || '-' %></p>
            </div>
          </div>
        </div>
        
        <% if (item.qr_code_path) { %>
          <div class="mt-8 border-t pt-6">
            <h3 class="text-xl font-semibold mb-4">QR Code</h3>
            <img src="<%= item.qr_code_path %>" alt="QR Code" class="w-32 h-32 mx-auto"/>
          </div>
        <% } %>
      </div>
    </div>
  </div>
</body>
</html>